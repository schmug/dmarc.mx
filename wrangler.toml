name = "dmarcheck"
main = "src/index.ts"
compatibility_date = "2026-06-30"
compatibility_flags = ["nodejs_compat", "nodejs_als"]
upload_source_maps = true

routes = [
  { pattern = "dmarc.mx", custom_domain = true },
]

# Workers Logs + Workers Traces (Cloudflare: "Production Workers without
# observability are a black box"). Sentry already captures exceptions and
# transactions, but it does not emit per-binding operation traces — which D1
# query in a request failed, how long a KV read took, whether a Durable Object
# RPC round-tripped. That is the signal the cron-degradation class of bug
# (#700 / #702) was hardest to diagnose without.
#
# The top-level `enabled` flag does NOT turn on traces: `observability.traces`
# has its own `enabled` key and must be set explicitly. Keep both.
#
# Top-level block only — NEVER add an [env.<name>] observability block here
# (an [env.*] block detached dmarc.mx on 2026-04-26, PRs #203 → #206).
#
# Must stay BELOW `routes`: in TOML every bare key after a [table] header
# belongs to that table, so hoisting this block above `routes` silently
# swallows the custom-domain route into [observability.traces].
#
# Traces vs. DNSBL_DQS_KEY (#728/#736): `queryDnsbl` (src/dns/client.ts) used
# to send the Spamhaus DQS key as a DoH URL query string, which Workers Traces
# would have recorded unredacted via `url.full`/`url.query` — the "SECRET
# COUPLING" this comment used to warn about. #732/#736 moved the lookup to an
# RFC 8484 DoH POST with the key in the body instead, so the key never touches
# a URL; that structural fix is the resolution this comment used to defer to,
# so the coupling no longer exists and DNSBL_DQS_KEY is safe to set with
# traces enabled.
[observability]
enabled = true

  # Sample every log line; volume at dmarc.mx's scale is well inside the
  # included allowance.
  [observability.logs]
  head_sampling_rate = 1

  # Traces are far heavier per event than logs, so sample 1%. Enough to
  # characterise a slow or failing cron run without paying for every request.
  [observability.traces]
  enabled = true
  head_sampling_rate = 0.01

[dev]
port = 8790

# Cloudflare Access enforcement for preview-branch deploys served on
# `*-dmarcheck.cory7593.workers.dev`. Inert on the production custom domain
# (`dmarc.mx`) — the middleware short-circuits on hostname before reading
# these. NOTE: top-level [vars] only — never add [env.<name>] blocks here
# (see feedback_workers_builds_env_pinning: doing so detached dmarc.mx on
# 2026-04-26 / PRs #203 → #206).
[vars]
ACCESS_AUD = "7b283d5d9b56f6fffa342280310a4ae68f13cbb05dc27f3c1d2faec2deae507a"
ACCESS_TEAM_DOMAIN = "coryrankin.cloudflareaccess.com"

# Self-host scoring rubric override (issue #25). A single JSON string of
# ScoringConfig knobs; absent or invalid → the shipped default rubric, so
# hosted dmarc.mx leaves this UNSET. Recognized keys (with defaults):
#   requireBimiForAPlus         (bool, true)  — A+ requires a BIMI record
#   requireMtaStsForAPlus       (bool, true)  — A+ requires enforcing MTA-STS
#   spfEfficientLookupThreshold (num,  5)     — SPF lookups ≤ this earn +1
#   dkimKeyMinBits              (num,  2048)  — keys below this incur −1
#   lowPctDowngradeThreshold    (num,  10)    — pct below this downgrades a tier
#   dkimRotationSelectorCount   (num,  2)     — this many selectors earn +1
# Example (self-hosters uncomment and edit):
# SCORING_CONFIG = '{"requireBimiForAPlus":false,"dkimKeyMinBits":4096}'

# Optional Spamhaus DQS (Data Query Service) key enabling the DNSBL/IP-reputation
# analyzer (issue #587). It is a SECRET — never set it here as a [vars] value and
# never commit a real key. Provision it out-of-band with:
#   wrangler secret put DNSBL_DQS_KEY
# Absent → the DNSBL analyzer is a clean no-op (informational, no grade impact),
# so self-host deploys and the test pool are unaffected. The key is embedded only
# in the outbound DQS query name (see queryDnsbl in src/dns/client.ts) and travels
# in an RFC 8484 DoH POST body, never a URL (#732/#736) — it is never logged,
# returned in responses, written into a cache key, or recorded by Workers Traces'
# `url.full`/`url.query` span fields. Safe to set with `[observability.traces]`
# enabled above.

# Per-invocation subrequest ceiling (issue #700). In workerd, node:dns is a DoH
# client that fetch()es cloudflare-dns.com per lookup, so EVERY DNS query is a
# Workers subrequest — roughly 52 per domain scanned. The nightly cron spends
# them cumulatively across domains in one invocation, and the Workers Paid
# default ceiling is 10,000: run 1788934679 died at 192 domains (192 x 52 ~=
# 10,000), after which every fetch() threw EBADQUERY for the rest of the
# invocation and 147 domains were false-graded D/F. MAX_DOMAINS_PER_RUN in
# src/cron/rescan.ts is the workaround for that ceiling; this raises the
# ceiling itself (Paid allows up to 10M).
#
# USAGE-MODEL PREMISE: Cloudflare documents [limits] as "only supported for the
# Standard Usage Model". A dashboard check on 2026-09-11 (Workers plans page,
# account f0fc4ca5b74274f7ba892e6c9ec411a7) found Paid advertising "Up to 5 min
# CPU time per request" — the Standard signature (legacy Bundled was 10 ms,
# Unbound 30 s). That is strong but INDIRECT: an inference from a plan-page
# string, not a literal usage-model readout, and it cannot be re-checked from
# this repo. Treat the ceiling as probably-raised, not proven-raised. On a
# legacy Bundled or Unbound model this block is accepted by Wrangler and then
# silently inert — no error, no warning, no deploy-time signal.
#
# Inert is safe on its own; it cannot make anything worse. What is NOT safe is
# raising MAX_DOMAINS_PER_RUN while this is inert — that reproduces #700
# exactly (147 domains false-graded D/F in one run). Confirm the ceiling
# actually moved first, by watching the cron.skipped Sentry tag (set in
# src/index.ts) fall toward zero across live cron runs.
#
# This is NOT the per-scan DNS budget. That is ScanBudget / DEFAULT_SCAN_LIMITS
# in src/dns/scan-budget.ts, a security control (GHSA-f828-8wf8-vqp2) bounding
# attacker-driven fan-out on a SINGLE scan. The two are unrelated; raising this
# one must never be read as license to relax that one.
#
# Top-level [limits] only — NEVER add an [env.<name>] block here (doing so
# detached dmarc.mx on 2026-04-26, PRs #203 → #206).
[limits]
subrequests = 100000

[triggers]
crons = ["17 6 * * *"]

# Atomic per-identity rate-limit counter (GHSA-v7qc-7qh8-h69g). The Cache API
# offered no atomic increment, so a concurrent burst under one identity could
# read the same stale count and collectively exceed the configured ceiling. A
# Durable Object serializes increments per identity (its single-threaded RPC),
# closing the race across isolates and colos. Optional at runtime:
# checkRateLimit() falls back to the in-memory limiter when this binding is
# absent (self-host deploys that strip it, and the Node test pool).
[[durable_objects.bindings]]
name = "RATE_LIMITER"
class_name = "RateLimiterDO"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["RateLimiterDO"]

[[d1_databases]]
binding = "DB"
database_name = "dmarcheck-db"
database_id = "7e6e7e64-5477-458f-b206-941ea58b7dba"
migrations_dir = "src/db/migrations"

# Cloudflare Email Sending for grade-drop alerts. alerts@dmarc.mx must be a
# verified sender in the Cloudflare dashboard before this binding delivers
# mail; the dispatcher catches E_SENDER_NOT_VERIFIED and skips gracefully.
[[send_email]]
name = "EMAIL"
allowed_sender_addresses = ["alerts@dmarc.mx"]

# Inbound test-email scanning (issue #417). Short-lived store (30-min TTL on
# every record) keyed by capability token: a "pending" reservation written on
# GET /check/email, overwritten with the parsed SPF/DKIM/DMARC verdict by the
# Email Worker `email()` handler when the test message arrives. This is the
# repo's FIRST KV namespace — the SSE scan cache (src/cache.ts) uses the Cache
# API, not KV.
#
# Top-level binding only — NEVER add an [env.<name>] block here (doing so
# detached dmarc.mx on 2026-04-26, PRs #203 → #206).
#
# Namespace created by the owner via `wrangler kv namespace create INBOX_TOKENS`.
# Remaining owner/zone-admin step (not committable): in Email Routing for
# dmarc.mx, create one rule `inbox@dmarc.mx` → "Send to a Worker" → dmarcheck and
# enable Subaddressing (Settings). Addresses are `inbox+<token>@dmarc.mx`; the
# specific rule outranks PhishSOC's zone catch-all, so only `inbox+*` reaches us.
[[kv_namespaces]]
binding = "INBOX_TOKENS"
id = "9194395b3f804f01bbbd14160b2a13bb"
